namespace VDC.AccessExtension.Core.DataTypes
{
    /// <summary>
    /// Identifies which of the three Valheim access lists an entry belongs to.
    /// </summary>
    public enum AccessListType
    {
        Whitelist = 0,
        Banlist   = 1,
        Adminlist = 2
    }
}
